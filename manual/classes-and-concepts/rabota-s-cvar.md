# Работа с CVar

Консольные переменные.

Если нужно сохранять игру с объектом, который хранит несериализуемый CVar — необходимо использовать модификатор `transient`. Он делает переменную невидимой для механизма сохранения.