# Из чего состоит проект

!!! abstract "TODO"
	_Ресурсы и код, форматы \*.wad, \*.pk3 и другие._



## Редактор

!!! abstract "TODO"
	В основном про Slade, можно добавить немного про менее известные редакторы.

Обычно используют [редактор Slade](https://slade.mancubus.net/index.php?page=downloads).

![](2.1_Интерфейс_Slade.png)
/// caption
Рисунок 1. Интерфейс редактора Slade
///

- В верхней части находится меню, блок быстрых действий и настройка базовых ресурсов.

- Слева располагается панель проектов (её можно включить или отключить через пункт меню "View" → "Archive Manager"). По умолчанию она делится по вертикали на проекты, открытые сейчас, и на недавние проекты.

- Правее находится список всех объектов внутри выбранного архива, в нём можно выбирать файл для редактирования. Снизу расположены поля фильтров.

- Справа — область просмотра и редактирования. В ней будет отображаться содержимое выбранного файла.


## Создание проекта и добавление ZScript

В начале нужно создать проект. Предварительная настройка Slade описана в [пока что не существующей статье](404). Напомним, что с проектом обычно работают, либо располагая его в отдельной директории, либо используя \*.pk3-архив, и в принципе это равнозначные варианты.

В следующих статьях будут описаны [способы работы без редактора](404). Однако Slade сразу предоставляет массу полезных возможностей (например, расцветку синтаксиса ZScript), так что в первом проекте лучше пользоваться им.


### Работа через архив PK3

Если Вы работаете через Slade — нужно нажать на "File" → "New" → "Archive" (или комбинацию клавиш `Ctrl + Shift + N`). Так как PK3 — на самом деле ZIP-архив, в открывшемся меню нужно выбрать создания нового ZIP, как показано на рис. 2:

![](2.1_Создание_pk3.png)
/// caption
Рисунок 2. Выбор формата ZIP, он же PK3
///

Затем можно сразу сохранить проект через "File" → "Save" или через комбинацию клавиш `Ctrl + S`.

После этого необходимо добавить в него файл кода ZScript — новый файл в проект добавляется либо через меню "Archive" → "New Entry", либо через комбинацию клавиш  `Ctrl + N`. Этот файл, как показано на рис. 3, должен располагаться в корневом каталоге ("/") и иметь тип "текст":

![](2.1_Создание_файла_ZScript.png)
/// caption
Рисунок 3. Создание файла ZScript
///

### Работа с директорией

**TODO**. Вкратце: создать в Проводнике директорию под проект, открыть в Slade через "File" → "Open Directory".

Для добавления файла кода ZScript можно:

- Создать новый файл через Проводник. В этом случае Slade сообщит об изменённых файлах и предложит выбрать те, которые нужно добавить в проект — соглашаемся со всеми;
- Также, как и в случае архива PK3, создать новый файл в самом Slade, через `Ctrl + N` (см. рис. 3).

### Об изменении файла ZScript

Проект создан, и в него добавлен головной файл кода ZScript. Код ZScript — это текст на (сравнительно) понятном человеку языке, которые по строгим правилам описывают поведение, требуемое от движка. При загрузке проекта движок переводит этот текст в собственные, внутренние команды.

Если работа происходит через Slade, то изменения при запуске движка будут видны только после сохранения всего проекта. Для этого, как упоминалось выше, можно использовать пункт меню "File" → "Save" или комбинацию клавиш `Ctrl + S`. В некоторых версиях Slade **перед** редактированием ещё придётся нажать на пункт "View as Text" в верхней-правой части панели просмотра (по умолчанию она находится слева).

Если же работа идёт через любой сторонний редактор текста (Блокнот, Notepad++, Visual Studio Code Editor и так далее), то Slade нужен больше для того, чтобы соединять всё в едином проекте. Здесь Slade снова будет предупреждать о том, что часть файлов изменилась, и нужно будет принимать все изменения в проект.

* * *

Можно считать, всё готово. Пора начинать [создавать первый мод](2.2-Простой_декоративный_актор.md).
